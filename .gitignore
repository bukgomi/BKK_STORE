node_modules
.next
out
.env
.env.local
.env.production
.env.docker
*.dump
*.log
.DS_Store
.idea
.vscode

# TypeScript / build artifacts
tsconfig.tsbuildinfo
*.tsbuildinfo

# Sentry
.sentryclirc

# 데이터 이관 산출물 (덤프/JSON 은 개인정보 포함 가능 → 커밋 금지)
scripts/migrate/out/
*.sql

# 업로드 파일(상품 이미지·채비도 사진 등)은 런타임 데이터 — 볼륨/R2 에 보관, 저장소에 커밋하지 않음
public/uploads/*
!public/uploads/.gitkeep
