# 작업지시서 — 운영 전환 준비 (개발 모드 → VPS 운영 모드)

> 이 문서는 Claude Code가 그대로 실행하기 위한 지시서다.
> 각 단계는 순서대로 진행하고, 단계마다 커밋한다. 파괴적 작업(볼륨 삭제, DB 리셋, 강제 푸시)은 반드시 사용자에게 먼저 묻는다.

## 0. 배경 (읽고 시작)

- 이 저장소(BKK_STORE)는 Next.js 14 + Prisma/PostgreSQL 16 + Redis 7(BullMQ 워커) 낚시용품 쇼핑몰이다.
- 지금까지는 `docker-compose.yml`(개발 모드: `next dev` + bind mount)로만 돌렸다.
- 목표: **국내 VPS 1대(4GB RAM, Ubuntu 22.04)에 Docker Compose로 운영 배포**할 수 있는 상태를 만든다.
- 기존 카페24 사이트의 상품·회원 데이터는 **이전하지 않는다.** 마이그레이션 스크립트 작업은 이번 범위에 없다.
- 도메인은 가비아에서 관리한다. DNS는 사용자가 직접 바꾼다. 여기서는 준비만 한다.
- 이미지 저장소는 Cloudflare R2(`STORAGE_PROVIDER=s3`)를 전제로 한다.

### 현재 파일 역할

| 파일 | 역할 | 이번 작업에서 |
|---|---|---|
| `docker-compose.yml` | 개발용 | 최소 수정 (1단계의 db push → migrate 교체만) |
| `docker-compose.public.yml` | 집 PC 임시 공개용 (DuckDNS/터널) | 손대지 않음. 운영에 쓰지 않는다 |
| `Dockerfile` | 운영 이미지 (멀티스테이지, 정상) | CMD 한 줄 교체 |
| `docker/Caddyfile` | DuckDNS DNS-01용 | 손대지 않음. 운영용은 별도 파일 신설 |
| `prisma/MIGRATIONS_PLAN.md` | 마이그레이션 전환 계획 | 1단계에서 읽고 반영 |

### 절대 규칙

1. 비밀값(비밀번호, 키, 토큰)을 저장소에 커밋하지 않는다. `.env.production`은 `.gitignore`에 넣는다. 예시 파일은 `.env.production.example`로 만들고 값은 비운다.
2. 운영 compose에서 **db와 redis의 `ports:`를 절대 공개하지 않는다.** Docker의 포트 공개는 ufw를 우회하므로 "방화벽으로 막으면 된다"는 가정은 금지.
3. 운영 시작 명령에 `prisma db push`를 쓰지 않는다. `prisma migrate deploy`만 쓴다.
4. 기존 개발 워크플로(`npm run docker:up`)가 계속 동작해야 한다. 각 단계 끝에 확인한다.
5. 모르는 값(도메인, 이메일, R2 버킷명 등)은 추측해 넣지 말고 `<PLACEHOLDER>`로 남기고 완료 보고에 목록으로 적는다.

---

## 1단계. Prisma 마이그레이션 체계 전환 (가장 중요)

**왜:** 현재 `Dockerfile` CMD와 `docker/entrypoint-dev.sh`가 `prisma db push`를 쓴다. `db push`는 스키마를 강제로 맞추면서 컬럼 삭제·재생성을 할 수 있어 운영 데이터가 유실될 수 있다.

### 할 일

1. `prisma/MIGRATIONS_PLAN.md`를 읽는다. 계획이 있으면 그 계획을 따르되, 아래 요구사항과 충돌하면 아래를 우선한다.
2. `prisma/migrations/` 폴더가 있는지 확인한다.
   - 없으면: 현재 `schema.prisma` 기준으로 **베이스라인 마이그레이션**을 만든다.
     - 개발 DB(compose의 `db` 서비스)를 띄운 상태에서 `npx prisma migrate dev --name init --create-only`로 SQL을 생성하고 내용을 검토한 뒤 적용한다.
     - 이미 `db push`로 만들어진 개발 DB에는 `npx prisma migrate resolve --applied <init 폴더명>`으로 "이미 적용됨" 처리한다. 개발 DB를 초기화해도 되는지는 사용자에게 먼저 묻는다.
   - 있으면: `npx prisma migrate status`로 스키마와 마이그레이션이 일치하는지 확인하고, 차이가 있으면 새 마이그레이션으로 잡는다.
3. `Dockerfile` CMD를 교체한다.
   - 변경 전: `npx prisma db push --skip-generate && npx next start -p 3000`
   - 변경 후: `npx prisma migrate deploy && npx next start -p 3000`
4. `docker/entrypoint-dev.sh`의 `prisma db push` 줄을 `npx prisma migrate deploy`로 바꾼다. (개발 중 스키마 변경은 호스트에서 `npm run db:migrate`로 마이그레이션을 만든 뒤 컨테이너 재시작으로 반영한다. 이 흐름을 README에 한 문단 추가한다.)
5. `package.json`의 `db:push` 스크립트는 남겨두되, README에 "운영·공유 DB에는 사용 금지" 경고를 적는다.

### 완료 기준

- `npx prisma migrate status`가 "Database schema is up to date" 를 출력한다.
- `npm run docker:down && npm run docker:up`으로 개발 환경이 정상 기동하고 로그인·상품 목록이 뜬다.
- `git grep "db push"` 결과에 운영 경로(Dockerfile, entrypoint)가 없다.
- 커밋: `chore(prisma): switch to migrate deploy, add baseline migration`

---

## 2단계. 운영용 Compose 작성

### 신규 파일

**`docker-compose.prod.yml`** — 단독으로 쓰는 완전한 파일 (base compose에 덧씌우지 않는다).

서비스:

- `db`: `postgres:16-alpine`. `ports` 없음. 비밀번호는 `.env.production`의 `POSTGRES_PASSWORD`. healthcheck 유지. `restart: unless-stopped`.
- `redis`: `redis:7-alpine`. `ports` 없음. `command: redis-server --requirepass ${REDIS_PASSWORD} --maxmemory 256mb --maxmemory-policy noeviction`. `restart: unless-stopped`.
- `app`: `build: .` (Dockerfile 사용). `ports` 없음 (Caddy만 접근). `DATABASE_URL`, `REDIS_URL`은 비밀번호 포함 형태로 조합. `DISABLE_INPROCESS_WORKER=true`. `restart: unless-stopped`. `depends_on`에 db/redis healthy 조건.
- `worker`: `app`과 같은 이미지(`image` 이름을 지정해 두 번 빌드하지 않게). `command: ["npm", "run", "worker"]`. `restart: unless-stopped`.
- `caddy`: 공식 `caddy:2-alpine` (DuckDNS 플러그인 불필요). `80:80`, `443:443`, `443:443/udp` 공개. `docker/Caddyfile.prod` 마운트. 볼륨 `caddy_data`, `caddy_config`. `restart: unless-stopped`.

공통:

- 모든 서비스에 로그 제한:
  ```yaml
  logging:
    driver: json-file
    options: { max-size: "10m", max-file: "5" }
  ```
- 볼륨: `pgdata`, `redisdata`, `caddy_data`, `caddy_config`. `uploads` 볼륨은 `STORAGE_PROVIDER=local` 대비용으로 넣되 주석으로 "R2 사용 시 불필요" 명시.

**`docker/Caddyfile.prod`**

```
{
  email {$ACME_EMAIL}
}

{$PUBLIC_DOMAIN} {
  encode zstd gzip
  reverse_proxy app:3000
}

www.{$PUBLIC_DOMAIN} {
  redir https://{$PUBLIC_DOMAIN}{uri} permanent
}
```

(HTTP-01 방식. 80/443이 열린 VPS에서 자동 발급된다.)

**`.env.production.example`** — 아래 키를 빈 값 또는 설명과 함께 나열. 실제 값은 넣지 않는다.

- `PUBLIC_DOMAIN`, `ACME_EMAIL`
- `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB`
- `REDIS_PASSWORD`
- `NEXTAUTH_URL`(=https://도메인), `NEXTAUTH_SECRET`, `ENCRYPTION_KEY` — **로컬에서 쓰던 `ENCRYPTION_KEY`를 그대로 가져와야 한다**는 주석 필수 (다르면 암호화된 회원 정보를 읽지 못함)
- `NEXT_PUBLIC_SITE_URL`, `ALLOWED_ORIGINS`
- `STORAGE_PROVIDER=s3` 및 R2 관련 키 (`.env.docker.example`에 있는 키 이름을 그대로 사용)
- PG 키 (`TOSS_*`, `INICIS_*`, `NAVERPAY_*`) — `.env.example` 기준
- `SENTRY_DSN` (선택)
- `IMAGE_REMOTE_HOSTS` — R2 공개 도메인 (3단계 참조)

### package.json 스크립트 추가

```
"prod:up":   "docker compose --env-file .env.production -f docker-compose.prod.yml up -d --build",
"prod:down": "docker compose --env-file .env.production -f docker-compose.prod.yml down",
"prod:logs": "docker compose --env-file .env.production -f docker-compose.prod.yml logs -f app worker caddy",
"prod:ps":   "docker compose --env-file .env.production -f docker-compose.prod.yml ps"
```

### 완료 기준

- `docker compose -f docker-compose.prod.yml config`가 오류 없이 통과한다 (`.env.production.example`을 복사한 임시 `.env.production`으로).
- `docker compose -f docker-compose.prod.yml config | grep -A3 "ports:"` 결과에 `5432`, `6379`, `3000`이 없다.
- 로컬에서 `npm run prod:up`으로 빌드·기동이 되고 (Caddy 인증서 발급은 실패해도 됨 — 로컬이므로), `docker compose exec app npx prisma migrate status`가 정상이다.
- 커밋: `feat(deploy): add production compose, Caddyfile.prod, env example`

---

## 3단계. Next.js 설정 보완

1. `next.config.js` `images.remotePatterns`: R2 공개 도메인은 `IMAGE_REMOTE_HOSTS` 환경변수로 넣는 구조가 이미 있다. `.env.production.example`의 해당 항목에 예시(`pub-xxxx.r2.dev` 또는 커스텀 도메인)를 주석으로 적는다. 코드 변경은 불필요하면 하지 않는다.
2. `output: "standalone"`은 **이번에 적용하지 않는다.** (워커가 `tsx scripts/worker.ts`로 `src/`와 devDependencies를 필요로 하므로 standalone 이미지에서 깨진다. 별도 워커 이미지를 만들 때 함께 검토.) 대신 `Dockerfile`의 runner 스테이지에서 불필요한 복사가 있는지만 검토하고 문제 없으면 그대로 둔다.
3. `.dockerignore`에 `.env*`(단 `.env.example`류 제외), `*.dump`, `tests/`, `.github/`, `tsconfig.tsbuildinfo`가 빠져 있으면 추가한다.

### 완료 기준

- `docker build -t fishing-mall .`가 성공하고 이미지 안에 `.env.docker`가 없다 (`docker run --rm fishing-mall ls -la /app | grep env` 로 확인).
- 커밋: `chore(docker): tighten dockerignore, document image hosts`

---

## 4단계. 백업 자동화

### 신규 파일

**`scripts/backup/pg-backup.sh`**

- `docker compose -f docker-compose.prod.yml exec -T db pg_dump -U $POSTGRES_USER -d $POSTGRES_DB -Fc` 결과를 `backups/fishing_mall_YYYYMMDD_HHMM.dump`로 저장.
- 저장 후 `rclone`(설치 여부 확인, 없으면 `rclone/rclone` 도커 이미지 사용)으로 R2 버킷의 `db-backups/` 경로에 업로드.
- 로컬은 7일, R2는 30일 보관 후 삭제.
- 실패 시 0이 아닌 종료 코드와 stderr 메시지. (알림 연동은 이번 범위 밖 — TODO 주석)

**`scripts/backup/pg-restore.sh`**

- 인자로 dump 파일 경로를 받아 `pg_restore --clean --if-exists --no-owner --no-privileges`로 복원.
- 실행 전 "운영 DB를 덮어씁니다. 계속하려면 데이터베이스 이름을 입력하세요" 확인 프롬프트.

**`docs/BACKUP.md`**

- cron 등록 줄 예시: `0 4 * * * cd /opt/bkk-store && ./scripts/backup/pg-backup.sh >> /var/log/bkk-backup.log 2>&1`
- rclone R2 리모트 설정 방법 (키 값은 placeholder).
- **복구 리허설 절차**: 별도 임시 Postgres 컨테이너에 최신 dump를 복원해 `SELECT count(*) FROM "User";` 등으로 확인하는 순서. "운영 시작 후 첫 주 안에 1회, 이후 분기 1회" 명시.

### 완료 기준

- 로컬 개발 DB를 대상으로 `pg-backup.sh`(R2 업로드는 `--no-upload` 옵션으로 스킵)와 `pg-restore.sh`가 왕복 성공한다.
- 커밋: `feat(ops): add pg backup/restore scripts and runbook`

---

## 5단계. 서버 초기 설정·배포 문서

**`docs/DEPLOY.md`** — 비전문가가 따라 할 수 있게 명령 단위로 쓴다.

1. VPS 준비: Ubuntu 22.04, 4GB RAM 이상, 공인 IP 1개.
2. 최초 접속 후 보안 기본:
   - 일반 사용자 생성 + sudo, SSH 키 등록, **비밀번호 로그인 비활성화**, 루트 SSH 비활성화
   - `ufw`: 22/80/443만 허용, 기본 deny. (compose에서 db/redis 포트를 공개하지 않으므로 Docker 우회 문제 없음 — 이 이유를 한 줄 적는다)
   - `unattended-upgrades` 활성화, `fail2ban` 설치
3. Docker 설치 (공식 스크립트), 사용자를 `docker` 그룹에 추가.
4. 코드 배치: `/opt/bkk-store`에 `git clone`. `.env.production` 생성, `chmod 600`.
5. 가비아 DNS: `@` A 레코드 → VPS IP, `www` A 레코드 → VPS IP. (TTL 300 권장. 전환 전날 미리 낮춰둘 것.)
6. 기동: `npm run prod:up` → `npm run prod:logs`로 마이그레이션·인증서 발급 확인.
7. 최초 관리자 시드: `docker compose -f docker-compose.prod.yml --env-file .env.production exec app npx tsx prisma/seed-admin.ts` (이후 `ADMIN_PASSWORD`는 .env에서 제거하거나 교체).
8. 백업 cron 등록 (4단계 문서 링크).
9. 업데이트 절차: `git pull && npm run prod:up` (이미지 재빌드 포함). 실패 시 이전 커밋으로 `git checkout <sha> && npm run prod:up`.
10. 점검 체크리스트:
    - `https://도메인` 접속, 자물쇠 정상
    - 회원가입 → 로그인 → 상품 담기 → 결제 테스트(PG 테스트 모드)
    - `docker compose ps` 전 서비스 `healthy`/`running`
    - 서버 재부팅 후 자동 기동 확인
    - 외부에서 `nc -zv <IP> 5432` 및 `6379`가 **실패**하는지 확인
    - 백업 파일이 R2에 생성되는지 확인

### 완료 기준

- 문서만 작성. 커밋: `docs(deploy): add VPS setup and deployment runbook`

---

## 6단계. 최종 점검 및 보고

1. `npm test` 통과.
2. `npm run docker:up`(개발) 정상, `npm run prod:up`(로컬 운영 리허설) 정상.
3. `git log --oneline`으로 단계별 커밋 5개 확인.
4. 아래 형식으로 완료 보고를 작성한다.

```
## 완료 보고
- 완료한 단계:
- 변경 파일 목록:
- 사용자가 채워야 할 값(<PLACEHOLDER> 목록):
- 사용자에게 확인이 필요한 결정:
- 다음 작업 제안:
```

---

## 범위 밖 (하지 말 것)

- 카페24 데이터 이전 스크립트 수정
- `output: standalone` 적용
- Vercel/Supabase 등 분리형 배포 구성
- 워커 알림(슬랙/이메일) 연동
- 포인트 정책·상품 등록 (별도 작업)
